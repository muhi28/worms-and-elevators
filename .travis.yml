language: android
jdk: oraclejdk8
sudo: false

android:
  components:
    - tools
    - platform-tools
    - tools
    - build-tools-24.0.2
    - android-22
    - android-24
    - sys-img-armeabi-v7a-android-22
    - extra-android-m2repository

before_install:
    - chmod +x gradlew
    
before_script:
  # Create and start emulator
  - echo no | android create avd --force -n test -t android-22 --abi armeabi-v7a
  - emulator -avd test -no-skin -no-audio -no-window &
  - android-wait-for-emulator
  - adb shell input keyevent 82 &

script:
  - ./gradlew  jacocoTestReport
  - ./gradlew sonarqube -Dsonar.organization=muhi28-github -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=03f2646172ccba66480a26a2188e727b563f4e0d
  
after_success:
  - bash <(curl -s https://codecov.io/bash) -t 586b642d-00c8-4f85-9650-53cbdf31ae9c
